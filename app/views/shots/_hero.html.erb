<% if !user_signed_in? %>
  <section class="hero is-dark">
    <div class="hero-body">
      <div class="container has-text-centered">
        <h1 class="title is-size-5">
          What are you blipping? <span class="has-text-grey-light">Blip is where you blip and see pictures. 
        </h1>
        <div class="content">
          <%= link_to "login", new_user_session_path, class: "button is-primary"%>
        </div>
      </div>
    </div>
  </section>
<%end%>